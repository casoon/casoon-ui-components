---
// Expliziter Import der Web-Komponente - wird nur in SSR verwendet
// und dient hier nur zur Typprüfung
import '../web-components/FeatureCard';

interface Props {
  icon: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;
---

<div class="feature-card-wrapper">
  <feature-card icon={icon} title={title} description={description}>
    <slot />
  </feature-card>
</div>

<!-- 
  Die Web-Komponente wird clientseitig registriert.
  In Astro sollte dies mit client:load oder ähnlichem Direktiven erfolgen.
-->
<script>
  // Expliziter Import mit relativem Pfad für bessere Browser-Kompatibilität
  import '../web-components/FeatureCard.js';
</script>

<style>
  .feature-card-wrapper {
    display: block;
  }
</style>